"use client";

const Loader = () => {
    return (
        <div className="flex items-center justify-center h-full">
            <div className="w-32 h-32 border-4 border-zinc-900 border-t-zinc-400 rounded-full animate-spin" />
        </div>
    );
};

export default Loader;
